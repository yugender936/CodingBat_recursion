public boolean array6(int[] nums, int index) {
  if(nums.length==0){
    return false;
  }
  int n = 0;
  if(index < nums.length){
   n = nums[index];
  }
  if((index<nums.length) && (n == 6)){
    return true;
  }
  if(index < nums.length){
  return array6(nums,index+1);
  }
  return false;
}
